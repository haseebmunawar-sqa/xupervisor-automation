# Copy this file to `.env` and fill in the values.
# `.env` is git-ignored and must NEVER be committed. In CI, set these as
# encrypted GitHub Actions secrets instead.

BASE_URL=https://dev.xupervisor.com

# Shared password for all roles (or set per-role *_PASSWORD below to override).
DEFAULT_PASSWORD=

ADMIN_EMAIL=
# ADMIN_PASSWORD=

EDITOR_EMAIL=
# EDITOR_PASSWORD=

VIEWER_EMAIL=
# VIEWER_PASSWORD=
